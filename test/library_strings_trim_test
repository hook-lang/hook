#!/bin/sh

. test/__test

title "Library strings: trim()"

script "use strings;
println(strings.trim(''));
println(strings.trim('   '));
println(strings.trim('   foo'));
println(strings.trim('bar   '));
println(strings.trim('   baz   '));"

expect_success "

foo
bar
baz"

run
