
var arr = [5, 2, 7, 3, 0, 1, 4, 6];

var n = len(arr);
var has_changed;
do {
  has_changed = false;
  n--;
  for (var i = 0; i < n; i++) {
    if (arr[i] > arr[i + 1]) {
      var t = arr[i];
      arr[i] = arr[i + 1];
      arr[i + 1] = t;
      has_changed = true;
    }
  }
} while (has_changed);

echo arr;
