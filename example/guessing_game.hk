//
// guessing_game.hk
//

use { random } in math;
use { StdIn, readln } in io;
use { trim } in strings;

println("Guess the number!");

val secret_number = int(1 + random() * 99);

loop {
  println("Please input your guess.");

  mut guess = readln(StdIn);

  guess = trim(guess);

  if (is_empty(guess))
    continue;

  guess = int(guess);

  println("You guessed: " + str(guess));

  val cmp = compare(guess, secret_number);
  
  if (cmp < 0)
    println("Too small!");
  else if (cmp > 0)
    println("Too big!");
  else {
    println("You win!");
    break;
  }
}
