//
// bubble_sort.hook
//

fn sort(arr) {
  let n = len(arr);
  let has_changed;
  do {
    has_changed = false;
    n--;
    for (let i = 0; i < n; i++) {
      if (arr[i] > arr[i + 1]) {
        const t = arr[i];
        arr[i] = arr[i + 1];
        arr[i + 1] = t;
        has_changed = true;
      }
    }
  } while (has_changed);
  return arr;
}

println(sort([5, 2, 7, 3, 0, 1, 4, 6]));
