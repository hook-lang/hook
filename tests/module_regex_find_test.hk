
use regex;
val pattern = "[a-zA-Z0-9_]+@[a-zA-Z0-9]+\.[a-zA-Z]{2,}";
val str = "foo#domain.com,bar@domain.com";
val re = regex.new(pattern);
val [start, end] = regex.find(re, str);
println(str[start .. end]);
