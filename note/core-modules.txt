
math:

  Pi: float

  abs(num: float) -> float
  sin(num: float) -> float
  cos(num: float) -> float
  tan(num: float) -> float
  asin(num: float) -> float
  acos(num: float) -> float
  atan(num: float) -> float
  floor(num: float) -> float
  ceil(num: float) -> float
  pow(base: float, power: float) -> float
  sqrt(num: float) -> float
  log(num: float) -> float
  exp(num: float) -> float
  random() -> float

os:

  ClocksPerSecond: float

  clock() -> float
  system(cmd: string) -> float
  getenv(var: string) -> string

io:

  StdIn: userdata
  StdOut: userdata
  StdErr: userdata
  SeekSet: float
  SeekCurrent: float
  SeekEnd: float

  open(filename: string, mode: string) -> nil|userdata
  close(stream: userdata) -> float
  popen(command: string, mode: string) -> nil|userdata
  pclose(stream: userdata) -> float
  eof(stream: userdata) -> bool
  flush(stream: userdata) -> float
  sync(stream: userdata) -> bool
  tell(stream: userdata) -> integer
  rewind(stream: userdata)
  seek(stream: userdata, offset: float, whence: float) -> float
  read(stream: userdata, size: float) -> nil|string
  write(stream: userdata, str: string) -> nil|float
  readln(stream: userdata) -> string
  writeln(stream: userdata, str: string) -> nil|float

strings:

  hash(str: string) -> integer
  lower(str: string) -> string
  upper(str: string) -> string
  trim(str: string) -> string
  starts_with(str1: string, str1: string) -> bool
  ends_with(str1: string, str1: string) -> bool

arrays:

  new_array(min_capacity: integer) -> array
  index_of(arr: array, elem: value) -> integer
  min(arr: array) -> value
  max(arr: array) -> value
  sum(arr: array) -> float
